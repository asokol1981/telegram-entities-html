coverage:
  status:
    project:
      default:
        target: 100%
        threshold: 0%
        informational: false
    patch:
      default:
        target: 100%
        threshold: 0%
        informational: false

comment:
  require_changes: true
  require_base: true
  require_head: true
  layout: "reach, diff, flags, files"

codecov:
  require_ci_to_pass: true
